/** * Quantum Optimizer Routes * Proxy routes to the Quantum Optimizer microservice (QAOA/QUBO) */const express = require('express');const router = express.Router();const { createServiceClient, healthCheck } = require('../utils/serviceClient');const { quantum: quantumConfig } = require('../config/services');const logger = require('../utils/logger');// Create quantum service clientconst quantumClient = createServiceClient(  'Quantum Optimizer',  quantumConfig.baseUrl,  quantumConfig.timeout);/** * POST /api/quantum/optimize * Run quantum optimization */router.post('/optimize', async (req, res, next) => {  try {    logger.info('Quantum optimization request received');    const response = await quantumClient.post(      quantumConfig.endpoints.optimize,      req.body    );    res.json({      success: true,      data: response.data,      service: 'quantum-optimizer',      timestamp: new Date().toISOString(),    });  } catch (error) {    next(error);  }});/** * POST /api/quantum/qubo * Solve QUBO problem */router.post('/qubo', async (req, res, next) => {  try {    logger.info('QUBO solve request received');    const response = await quantumClient.post(      quantumConfig.endpoints.qubo,      req.body    );    res.json({      success: true,      data: response.data,      service: 'quantum-optimizer',      timestamp: new Date().toISOString(),    });  } catch (error) {    next(error);  }});/** * POST /api/quantum/qaoa * Run QAOA algorithm */router.post('/qaoa', async (req, res, next) => {  try {    logger.info('QAOA request received');    const response = await quantumClient.post(      quantumConfig.endpoints.qaoa,      req.body    );    res.json({      success: true,      data: response.data,      service: 'quantum-optimizer',      timestamp: new Date().toISOString(),    });  } catch (error) {    next(error);  }});/** * POST /api/quantum/classical * Run classical optimization (fallback) */router.post('/classical', async (req, res, next) => {  try {    logger.info('Classical optimization request received');    const response = await quantumClient.post(      quantumConfig.endpoints.classical,      req.body    );    res.json({      success: true,      data: response.data,      service: 'quantum-optimizer',      method: 'classical',      timestamp: new Date().toISOString(),    });  } catch (error) {    next(error);  }});/** * GET /api/quantum/health * Check quantum service health */router.get('/health', async (req, res) => {  const health = await healthCheck(quantumClient, quantumConfig.endpoints.health);    res.status(health.healthy ? 200 : 503).json({    service: 'quantum-optimizer',    ...health,    timestamp: new Date().toISOString(),  });});module.exports = router;